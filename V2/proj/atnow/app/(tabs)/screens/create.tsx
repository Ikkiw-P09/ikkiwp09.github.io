import React from 'react';
import { View, Button } from 'react-native';

const CreateScreen = () => {
  return (
    <View>
      <Button title="Create Story" onPress={() => {}} />
      <Button title="Create Short" onPress={() => {}} />
      <Button title="Create Post" onPress={() => {}} />
    </View>
  );
};

export default CreateScreen;
